import { WhiteLabelData } from "@web3auth/auth";
import { NFT_CHECKOUT_BUILD_ENV_TYPE } from "./enums";
export declare class NFTCheckoutEmbed {
    clientId: string;
    isInitialized: boolean;
    private modalZIndex;
    private buildEnv;
    private readonly embedNonce;
    constructor({ modalZIndex, clientId }: {
        modalZIndex?: number;
        clientId: string;
    });
    init(params?: {
        buildEnv?: NFT_CHECKOUT_BUILD_ENV_TYPE;
        whiteLabel?: WhiteLabelData;
    }): Promise<void>;
    show({ receiverAddress, contractId }: {
        receiverAddress?: string;
        contractId: string;
    }): void;
    hide(): void;
    cleanup(): void;
    private getIframe;
}
